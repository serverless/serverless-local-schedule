{
  "name": "serverless-local-schedule",
  "version": "0.1.6",
  "description": "Allows defining CloudWatch schedules in local timezones(with DST!) in serverless events",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier -l index.js || (echo need formatting ; exit 1)",
    "format": "prettier --write index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unitedincome/serverless-local-schedule.git"
  },
  "keywords": [
    "serverless",
    "cron",
    "schedule",
    "aws",
    "lambda",
    "cloudwatch"
  ],
  "author": "UnitedIncome <engineering@unitedincome.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/unitedincome/serverless-local-schedule/issues"
  },
  "homepage": "https://github.com/unitedincome/serverless-local-schedule#readme",
  "dependencies": {
    "local-crontab": "^0.1.7"
  },
  "devDependencies": {
    "prettier": "*"
  }
}
